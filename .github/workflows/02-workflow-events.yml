name: 02 - Eventos
on:
  push:
  pull_request:
  schedule:
    - cron: '0 18 3 3 *'
  workflow_dispatch:
jobs:
  ShowTrigger:
    runs-on: ubuntu-latest
    steps:
      - name: echo-trigger-event
        run: |
          echo "Event name: ${{ github.event_name }}"