name: 02 - Eventos
on:
  push:
  pull_request:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
    - name: echo-trigger-event
      run: echo "Event triggered: ${{ github.event_name }}"